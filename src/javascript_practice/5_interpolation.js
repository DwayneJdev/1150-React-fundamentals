const username = 'kevin';
console.log(`${username} is the true JS Wizard!`);

console.log(`Hey students, 
what bugs can ${username} fix for you today`);

const person = {
    username: 'Dwayne',
    profession: 'Yeeter'
};
console.log(`${person.username}, ${person.profession}, extraordinaire`); // Dwayne, Yeeter, Extraordinaire